# Core analysis dependencies.
#
# llama-cpp-python is deliberately NOT listed here.  The correct build depends
# on the backend (a plain `pip install llama-cpp-python` gives a CPU-only
# wheel, which would silently run on the host CPU on a GPU pod).  Install it
# with:
#     bash scripts/install_engine.sh
psutil>=5.9.0
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
tqdm>=4.65.0
huggingface-hub>=0.21.0
datasets>=2.14.0

# NVML bindings; used to measure VRAM and verify full GPU offload.
# Harmless to install on machines without an NVIDIA GPU.
nvidia-ml-py>=12.535.77
